/*
 * # Filter
 * The filter function takes a "predicate" function and an array,
 * applies the predicate function to each value in the array,
 * and returns a new array with only those values for which the predicate function returns true.
 */

/*
 * predicate function:
 *   a function that takes in a value and returns a boolean
 */

function filter() {
  //@todo your code goes here
}

export default filter;
